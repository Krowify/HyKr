Drop the Minimal theme's wallpaper here as `minimal.jpg` (referenced by
`../theme.json`'s `default_wallpaper`). Not included in the repo --
Claude Code's remote sandbox can't save images attached in chat to disk.

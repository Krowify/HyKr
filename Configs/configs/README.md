# configs

Dotfiles destined for `~/.config`. Each subfolder here should mirror the
name of the application it configures (e.g. `hypr/`, `waybar/`, `kitty/`)
so it can be symlinked or stowed directly into `~/.config/<app>`.
